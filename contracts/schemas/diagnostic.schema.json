{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eca-rule-engine.example/schemas/v1/diagnostic.schema.json",
  "title": "ECA diagnostic",
  "description": "A machine-readable model-validation or evaluation-error diagnostic.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "code",
    "message"
  ],
  "properties": {
    "code": {
      "description": "Stable diagnostic category, including ambiguous-action-selection for an undefined query.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096
    },
    "pointer": {
      "description": "JSON Pointer into the document that caused the diagnostic, when one location applies.",
      "type": "string"
    }
  },
  "examples": [
    {
      "code": "ambiguous-action-selection",
      "message": "Event 'signal.received' matched distinct actions [alert, record] through rules [alert.high.1, alert.high.2, record.signal].",
      "pointer": "/type"
    }
  ]
}
